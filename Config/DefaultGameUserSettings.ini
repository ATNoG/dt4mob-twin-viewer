; Defaults used only until a machine has its own Saved/Config/.../GameUserSettings.ini
; (i.e. first run on a fresh install). After that, the user's saved settings win, so
; changing this later won't retroactively fix machines that already ran once.
[/Script/Engine.GameUserSettings]
FrameRateLimit=60.000000
; Scalability levels (AntiAliasing/GlobalIllumination/Reflection/Resolution quality) used to
; live here too, but two attempts (flat keys, then the ScalabilityQuality=(...) struct format)
; both failed to actually apply — console history for sg.AntiAliasingQuality showed only
; "Constructor" either way. They're set directly as sg.* cvars in DefaultEngine.ini's
; [SystemSettings] block instead; see the comment there for why that path works reliably here.
